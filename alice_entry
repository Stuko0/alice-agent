#!/usr/bin/env python3
"""
Alice Agent CLI launcher.

This wrapper should behave like the installed `alice` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from alice_cli.main import main
    main()
